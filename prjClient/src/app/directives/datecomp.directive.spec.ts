import { DatecompDirective } from './datecomp.directive';

describe('DatecompDirective', () => {
  it('should make instance to be created', () => {
    const dir = new DatecompDirective();
    expect(dir).toBeTruthy();
  });
});
